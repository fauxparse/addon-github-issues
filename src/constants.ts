export const ADDON_ID = 'storybook/addon-github-issues';
export const TAB_ID = `${ADDON_ID}/tab`;
export const PARAM_KEY = `issues`;

export const EVENTS = {
  RESULT: `${ADDON_ID}/result`,
  REQUEST: `${ADDON_ID}/request`,
  CLEAR: `${ADDON_ID}/clear`,
};
